import React from 'react'
import ReactDOM from 'react-dom/client'
import IndexPage from './components/IndexPage'

ReactDOM.createRoot(document.getElementById('react-index-page')).render(
    <React.StrictMode>
        <IndexPage />
    </React.StrictMode>,
)
