import React from 'react'
import ReactDOM from 'react-dom/client'
import Contact from './components/Contact'

ReactDOM.createRoot(document.getElementById('react-contact-page')).render(
    <React.StrictMode>
        <Contact />
    </React.StrictMode>,
)
