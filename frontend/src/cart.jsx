import React from 'react'
import ReactDOM from 'react-dom/client'
import CartPage from './components/CartPage'

ReactDOM.createRoot(document.getElementById('react-cart-page')).render(
    <React.StrictMode>
        <CartPage />
    </React.StrictMode>,
)
