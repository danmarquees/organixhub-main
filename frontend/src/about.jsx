import React from 'react'
import ReactDOM from 'react-dom/client'
import About from './components/About'

ReactDOM.createRoot(document.getElementById('react-about-page')).render(
    <React.StrictMode>
        <About />
    </React.StrictMode>,
)
